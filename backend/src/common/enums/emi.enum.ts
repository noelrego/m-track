export enum EmiAmountMode {
  Monthly = 'monthly',
  Total = 'total',
}

export enum EmiEditScope {
  Single = 'single',
  Future = 'future',
}

export enum EmiPlanStatus {
  Active = 'active',
  Stopped = 'stopped',
}
